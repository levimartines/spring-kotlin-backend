spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres

### LOGGING ###
logging:
  level:
    root: debug

jwt:
  secret: testSecret
  expiration: 3600000